protocol bgp be_public_henet from be_public {
  neighbor 2001:470:12:4d::1 as 6939;
  import filter {
    if public_is_self_net() then reject;
    accept;
  }
  export filter {
    if public_is_self_net then {
      accept;
    }
    reject;
  }
};
