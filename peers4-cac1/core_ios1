protocol bgp b_e_core_ios1 from b_e_core {
  neighbor 10.248.217.49 as 65248;
  import filter {
    if is_core() then accept;
    reject;
  };
  import filter {
    if is_core() then accept;
    reject;
  };
};
